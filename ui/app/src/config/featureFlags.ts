/**
 * Blog-to-video (Workspace toolbar, /videos hub, Site editor Video tab).
 * When true: also uncomment the Videos import + `/videos` route in `App.tsx`, and the Videos
 * item in `AppSidebar.tsx` (search for FEATURE_VIDEOS_UI in those files).
 */
export const FEATURE_VIDEOS_UI = false;
